<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jannada.bia">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>


                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".Recetas.DesayunoActivity" />
        <activity android:name=".Recetas.CenaActivity" />
        <activity android:name=".Recetas.AlmuerzoActivity" />
        <activity android:name=".Recetas.LigthActivity" />
        <activity android:name=".Recetas.EnsaladaActivity" />
        <activity android:name=".Recetas.BebidasActivity" />
        <activity android:name=".Recetas.PostreActivity" />
        <activity android:name=".Recetas.RapidaActivity"></activity>
        <activity android:name=".Recetas.Detalle"></activity>
        <activity android:name=".AgregReceta"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>